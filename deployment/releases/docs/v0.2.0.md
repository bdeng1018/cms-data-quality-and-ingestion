# 0.2.0 — 2026-07-23

## Added

- Stage 05 pipeline runner coordinating Stages 01–04 into a deterministic workflow.
- `pipeline_summary.json` capturing timestamps, stage statuses, warnings, and duration.
- Stage 05 validation layer ensuring Stage 04 artifacts are complete and consistent.
- Multi‑stage diagnostics verifying Stage 02 → Stage 03 → Stage 04 continuity.
- Makefile orchestration targets (`run`, `smoke`, `diag-intermediate`).
- Documentation updates covering Stage 05 usage, outputs, and integration points.
- Unified logging for runner + diagnostics.

## Changed

- Normalized `facility_id` dtype across Stage 03 and Stage 05 diagnostics.
- Improved Stage 03 → Stage 04 consistency checks for column profiles and facility metrics.
- Refined Makefile workflow to support full‑pipeline execution.
- Updated root README to reflect Stage 05 completion.
- Standardized directory structure for intermediate and final artifacts.

## Fixed

- False `facility_id` mismatches caused by mixed integer/string types.
- Stage 05 runner edge cases (missing artifacts, invalid paths, inconsistent manifests).
- Makefile diagnostic invocation errors for Stage 02 QIES ingestion.
- Logging inconsistencies across ingestion, profiling, and reporting layers.

## Removed

- No removals in this release.

## Deprecated

- No deprecated components in this release.

## Security

- Improved logging consistency and error handling.
- Added basic runner‑level validation to prevent malformed inputs.

## Observability

- Added runner logs and multi‑stage diagnostics.
- Improved reporting of drift indicators and sparsity warnings.

## Provenance

- Updated provenance fields:
  - `pipeline_version: 0.2.0`
  - `schema_version`
  - `artifact_version`
  - `manifest_version`
  - `deployment_version: 0.0.0`
