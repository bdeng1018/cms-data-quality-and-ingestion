name: pos_qies_pipeline
channels:
  - conda-forge

dependencies:
  - python=3.11

  # Core developer tooling
  - black
  - isort
  - pytest
  - ruff

  # Data handling (Stage 02 ingestion + Stage 03/04 profiling)
  - pandas
  - pyarrow
  - requests

  # Pip-only packages (none yet for Stage 01–04)
  - pip:
      - ""
