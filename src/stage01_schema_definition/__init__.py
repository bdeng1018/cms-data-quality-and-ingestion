"""
Stage 01 — Schema Definition & Validation

This package provides the canonical schema loader and validator used
throughout the CMS Data Quality & Ingestion pipeline.

Modules included:
- schema_loader: loads the canonical JSON schema
- schema_validator: performs structural and type validation

These components form the foundation of the CMS Data Quality &
Ingestion pipeline and are used by diagnostics, tests, and all
downstream ingestion stages.
"""
