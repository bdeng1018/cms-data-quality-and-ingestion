[pytest]
# Where tests live
testpaths = tests

# Test file naming convention
python_files = test_*.py

# Ensure src/ is importable for Stage 01–04 modules
pythonpath = src

# Quiet mode + show warnings
addopts = -q -W default